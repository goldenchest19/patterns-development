spring.application.name=currency-rate-provider
server.port=${SERVER_PORT:0}
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=${SHUTDOWN_TIMEOUT:30s}

grpc.server.port=${GRPC_SERVER_PORT:50051}
grpc.server.shutdown-grace-period=${GRPC_SERVER_SHUTDOWN_GRACE_PERIOD:30s}

spring.cloud.zookeeper.connect-string=${ZOOKEEPER_CONNECT_STRING:localhost:2181}
spring.cloud.zookeeper.discovery.root=${ZOOKEEPER_DISCOVERY_ROOT:/services}
spring.cloud.zookeeper.discovery.register=true
spring.cloud.zookeeper.discovery.instance-port=${grpc.server.port}
spring.cloud.service-registry.auto-registration.enabled=false

rate.base-value=${RATE_BASE_VALUE:90.0}
rate.spread=${RATE_SPREAD:1.0}

management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.prometheus.access=unrestricted
management.endpoint.health.access=unrestricted
management.endpoints.access.default=unrestricted
management.server.port=${MANAGEMENT_SERVER_PORT:8081}
management.metrics.tags.application=${spring.application.name}
